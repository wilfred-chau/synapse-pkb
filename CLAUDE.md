# PKB 项目核心规则（CLAUDE.md）

本文件是本仓库的核心开发约束，供后续会话与日常开发统一遵循。

## 1. 项目定位与目标

- 项目名称：个人知识库系统（PKB, Personal Knowledge Base with Proactive Memory）。
- 项目性质：个人独立开发 / AI 全栈技能沉淀项目。
- 技术栈基线：Spring Boot 3、React 18、PostgreSQL 16（pgvector）、Redis、Docker Compose。
- 核心差异点：不是普通笔记/RAG 工具，而是以"主动关联"为核心的个人长期记忆系统。
- 最高优先级：严格遵循需求规格说明书实现，不擅自扩展范围，不把"想做"混入"本期要做"。

## 2. 范围与需求约束

- 当前权威需求文档：
  - `F:\synapse-pkb\docs\requirements\个人知识库系统PKB需求规格说明书.docx`
  - `F:\synapse-pkb\docs\requirements\requirements-full.md`
- 需求优先级高于临时推测；若实现与需求冲突，以需求文档为准。
- 本期范围聚焦：
  - 基础平台
  - 内容处理与分类感知摘要
  - 检索
  - 主动关联引擎
  - 主动呈现
- 明确排除项：语音备忘录属于二期，不在一期开发范围内，除非用户明确要求切入二期。
- 若用户只问"某模块/某编号是什么、范围是什么"，优先按需求文档直接回答，不进入实现流程。

## 3. 开发值守规则

### 3.1 doc-pilot 是本项目默认值守 skill

任何开发、bug 修复、重构、测试、继续开发、模块实现任务，开工前先使用 doc-pilot skill：

- skill 名称：`doc-pilot`
- 实际路径以 `docs/.doc-pilot.json` 的 `skills_registry` 字段为准，本文件不重复维护
  具体路径——这样宿主 Agent 工具变化时，只需改一处配置。

触发要求：

- 若提示词命中了开发/修复/整理文档等语义，默认主动按 doc-pilot 路由执行。
- 若未自动触发，也要手动执行同等流程，不能跳过。

### 3.1.1 提示词体验原则

- 本项目默认由 agent 自主判断应走的 skill 与流程模式，不把流程编排负担压给开发者。
- 开发者通常只需用自然表达描述任务，例如"开发需求文档里的 A1 部分""修复首页白屏问题"。
- agent 需根据任务语义自行判断任务分级（L1～L4，见 doc-pilot skill 定义）与对应工作流，
  并在回复中用一句话说明判断结果，不需要展开解释，但不能完全隐藏这一步判断。
- 只有当开发者明确提出"先给方案""直接改""不要动某部分"等额外要求时，再将这些要求作为
  流程控制信号，优先级高于 agent 的自主判断。

### 3.2 首次使用 doc-pilot 的特殊约束

当前仓库已初始化 doc-pilot 知识库，`docs/.doc-pilot.json` 已存在。

因此后续触发 doc-pilot 时，直接按常规值守路由执行即可，不再重复走"模式 C：初始化知识库"。

### 3.3 开工前固定动作

若任务属于开发/修复/重构/测试：

1. 若仓库已进入 doc-pilot 管理态，先按其规则检查分支同步状态。
2. 读取知识库入口文档后再动手，不先写代码后补读文档。
3. 判断任务分级；若属于 L3/L4，且实现路径仍需对齐，先在 doc-pilot 值守下触发
   `plan-before-code` 生成并确认方案，再进入建票或开发；未经方案确认，不得进入 L3/L4
   任务的正式实现，除非用户明确要求跳过并已被告知风险。
4. 若任务预计跨会话，或用户明确要求暂停稍后继续，按 doc-pilot 规则创建跨会话任务记录；
   L4 任务无论是否预计跨会话都必须创建。
5. 修 bug 前先检索历史 bug 记录，避免重复踩坑。

### 3.3.1 数据库相关任务的固定执行规范

- 只读类数据库操作：若只是查询、核对、读取数据库信息，优先使用已配置在 MCP 中的数据库连接
  直接完成查询与获取。
- 写入类数据库操作按性质分两种对待：
  - **结构性变更**（建表、改字段类型、加/删索引、执行迁移脚本等 DDL 操作）：不通过 MCP
    直接落库，统一通过远程连接登录 CentOS 服务器，在服务器本地执行；执行前必须先把即将
    运行的语句和影响范围说清楚，确认无误后再落地。
  - **普通数据增删改**（DML）：同样统一在 CentOS 服务器本地执行，完成必要的验证准备
    （如种子数据设计）后可以直接执行，不需要逐条确认，视为已获授权。
- 验证要求：所有数据库改动都必须同步准备符合业务逻辑的种子数据，并通过导入种子数据来完成
  功能验证，不能只做结构修改而不验证实际数据链路。
- 结果回报：涉及数据库改动的任务收工时，需明确说明执行了哪些数据库命令（结构性变更需附上
  实际执行的语句）、导入了哪些种子数据、验证了哪些结果。

### 3.4 skill 协作关系

- `doc-pilot` 是全程值守角色，负责项目上下文持续读取、任务分级路由、规范监督、跨会话任务
  记录与知识库写回，不是一次性动作。
- `plan-before-code` 在 `doc-pilot` 值守下工作，负责把 L3/L4 需求转化为可确认方案。
- `dev-with-ticket` 在 `doc-pilot` 值守下工作，负责基于需求或已确认方案创建 Jira ticket，
  并在用户确认后进入开发。
- 三者详细职责边界见 `doc-pilot` skill 的 `references/skill-contract.md`，本文件不重复
  定义，避免多处维护导致不一致。
- 若任务先经过 `plan-before-code`，则 Jira ticket 的 Description 优先使用方案摘要，而不是
  直接复用原始需求文本。
- 若任务经过 `plan-before-code`，必须先在 `docs/plans-local/<task>/` 生成本地方案目录，
  至少包含 `plan.md` 与 `checklist.md`；该目录只用于本地协作沉淀，永远不上传到代码仓库。

### 3.5 收工后固定动作

完成开发或修复后，必须：

1. 按 doc-pilot 模板回写开发日志或 bug 记录。
2. 刷新 `STATUS.md`。
3. 若架构或横切机制变化，更新架构快照。
4. 若本次任务创建了跨会话任务记录，将其标记为已完成/已暂停/已阻塞之一。
5. 在汇报中提供可直接粘贴的 commit message 草案，并列出本次建议 `git add` 的文件清单，
   但不代执行 git add / commit / push。
6. 若任务已通过 Jira 建票，commit message 草案需带 ticket 编号前缀（如 `[SPKB-1] feat xxx`）。

### 3.5.1 改码后的本地强制自检

- 以后凡是改动代码，无论是功能开发还是 bug 修复，收工前都必须先完成本地编译/构建通过，
  不能只做静态修改不验证。
- 后端至少要完成一次可通过的本地编译或测试，并实际启动服务做自检。
- 前端至少要完成一次可通过的本地构建，并实际启动开发服务做自检。
- 自检口径默认包含：前后端服务均可启动、前端页面可访问、核心改动链路可人工验证；若涉及
  接口联调，则必须实际联调而不是只看单侧日志。
- 若因为环境变量、外部服务、端口占用等原因导致本地启动失败，必须先继续排查；确实无法消除
  时，要在汇报中明确说明阻塞原因、已尝试动作和当前未完成的验证项，不能直接把任务表述为
  "已完成"；若创建了跨会话任务记录，应标记为"验证未通过"。

## 4. 文档与知识库原则

- 文档不是附属品，而是项目的一部分；开发过程必须维护项目知识库。
- 根目录散落的新 Markdown 文档不长期保留；正式接入 doc-pilot 后应归档到 `docs/`。
- 需求原文与需求快照都要保留：
  - docx 是 source of truth
  - md 是 AI 可检索快照
- 若需求文档更新，后续应同步更新 md 快照，避免答非所问或基于过期需求开发。

## 5. 实现原则

- 严格遵循规格，不凭猜测补功能。
- 优先做局部、精确、可验证的修改，避免无关重构。
- 先交付能工作的最小闭环，再做增强，不追求一步到位的大而全实现。
- 配置驱动优先于硬编码，尤其是分类模板、摘要抽取、关联判定阈值等需求中已明确具备扩展性的
  部分；这条原则同样适用于项目本身的规则文件——路径、工具依赖等信息优先放进配置文件，
  而不是散落写死在多份文档里。
- 单用户系统假设成立前，不引入多租户、复杂权限、企业级分布式治理等超范围设计。

## 6. 技术决策边界

- 检索层优先使用 PostgreSQL + pgvector + 全文检索，不擅自引入 Elasticsearch、Milvus、
  Qdrant 等额外基础设施，除非用户明确要求。
- 调度与异步优先沿用需求文档中的 `Spring @Scheduled + Redis` 思路，不擅自升级到
  Kafka/RabbitMQ。
- 部署默认面向单机 Docker Compose，不提前引入 Kubernetes。
- 涉及数据库的读写边界见 3.3.1 节。
- 涉及第三方库、框架 API、枚举值、方法签名时，必须基于真实代码或真实文档核实；不能靠印象
  编码。

## 7. 用户协作约束

- 非必要不追问；能做合理假设时直接推进，并在结果中说明假设。
- 当存在非显然后果的方案分叉时，再暂停与用户确认。
- 不替用户执行 git add / commit / push，除非用户明确要求。
- 每次代码改动完成后，都要附上英文 commit message 草案，并列出本次建议 `git add` 的文件
  清单；若已有 Jira ticket，commit message 需带 ticket 编号前缀。
- 保持行动优先：不要在没有阻断证据时反复推翻已选技术路径。

## 8. 执行风格：拒绝过度思考，行动优先

适用前提：这条约束适用于技术实现决策效率，不适用于值守流程本身。像"先读文档""先查 bug
历史""判断任务分级"这类流程性动作必须执行，不属于过度思考。

具体要求：

- 不在开始前做超长推演，优先拆小步推进。
- 有可落地路径时先产出代码或文档，不空转。
- 自动化验证以覆盖核心链路为主，不盲目追求过度测试。
- 一旦确定实现路径，除非遇到新的阻断性证据，否则不要反复推翻。

## 9. 长任务上下文保护

若任务持续时间长、上下文即将膨胀，按 doc-pilot 规则更新跨会话任务记录（如已创建），并
主动建议用户开启新会话，提供可直接复用的上下文摘要提示词。摘要内容至少包括：

- 项目核心技术栈
- 当前任务进度（及任务 ID/工单号，如有）
- 已知坑点与约束
- 下一步计划

## 10. 本项目当前已知事实

- `doc-pilot` 及其协作 skill（`plan-before-code`、`dev-with-ticket`）的实际路径，登记在
  `docs/.doc-pilot.json` 的 `skills_registry` 字段，不在本文件重复维护，避免路径变化时
  多处修改遗漏。
- 当前 `docs/` 知识库已初始化，并由 `docs/.doc-pilot.json` 管理。
- 当前根目录仍保留需求文档原件与其 Markdown 快照，路径见第 2 节。
- 本项目已配置 Jira Cloud 站点 `wilfredchau.atlassian.net`，项目 Key 为 `SPKB`。

如后续项目结构或流程发生变化，应优先更新本文件；若变化的是某个 skill 的具体路径，改
`docs/.doc-pilot.json` 即可，无需改动本文件。
