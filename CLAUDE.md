# PKB 项目核心规则（CLAUDE.md）

本文件是本仓库的核心开发约束，供后续会话与日常开发统一遵循。

## 1. 项目定位与目标

- 项目名称：个人知识库系统（PKB, Personal Knowledge Base with Proactive Memory）。
- 项目性质：个人独立开发 / AI 全栈技能沉淀项目。
- 技术栈基线：Spring Boot 3、React 18、PostgreSQL 16（pgvector）、Redis、Docker Compose。
- 核心差异点：不是普通笔记/RAG 工具，而是以“主动关联”为核心的个人长期记忆系统。
- 最高优先级：严格遵循需求规格说明书实现，不擅自扩展范围，不把“想做”混入“本期要做”。

## 2. 范围与需求约束

- 当前权威需求文档：
  - `F:\personal-knowledge-base\个人知识库系统PKB需求规格说明书.docx`
  - `F:\personal-knowledge-base\个人知识库系统PKB需求规格说明书.md`
- 需求优先级高于临时推测；若实现与需求冲突，以需求文档为准。
- 本期范围聚焦：
  - 基础平台
  - 内容处理与分类感知摘要
  - 检索
  - 主动关联引擎
  - 主动呈现
- 明确排除项：语音备忘录属于二期，不在一期开发范围内，除非用户明确要求切入二期。
- 若用户只问“某模块/某编号是什么、范围是什么”，优先按需求文档直接回答，不进入实现流程。

## 3. 开发值守规则

### 3.1 doc-pilot 是本项目默认值守 skill

任何开发、bug 修复、重构、测试、继续开发、模块实现任务，开工前先使用 doc-pilot skill：

- 当前 skill 路径：`.trae/skills/doc-pilot-v2/`
- skill 名称：`doc-pilot`

触发要求：

- 若提示词命中了开发/修复/整理文档等语义，默认主动按 doc-pilot 路由执行。
- 若未自动触发，也要手动执行同等流程，不能跳过。

### 3.2 首次使用 doc-pilot 的特殊约束

当前仓库尚未初始化 doc-pilot 知识库（`docs/.doc-pilot.json` 不存在）。

因此第一次正式触发 doc-pilot 时，必须先走“模式 C：初始化知识库”，再继续原任务，不允许假设 `docs/` 已经存在。

### 3.3 开工前固定动作

若任务属于开发/修复/重构/测试：

1. 若仓库已进入 doc-pilot 管理态，先按其规则检查分支同步状态。
2. 读取知识库入口文档后再动手，不先写代码后补读文档。
3. 修 bug 前先检索历史 bug 记录，避免重复踩坑。

### 3.4 收工后固定动作

完成开发或修复后，必须：

1. 按 doc-pilot 模板回写开发日志或 bug 记录。
2. 刷新 `STATUS.md`。
3. 若架构或横切机制变化，更新架构快照。
4. 在汇报中提供可直接粘贴的 commit message 草案，并列出本次建议 `git add` 的文件清单，但不代执行 git add / commit / push。
5. 若任务已通过 Jira 建票，commit message 草案需带 ticket 编号前缀（如 `[SPKB-1] feat xxx`）。

## 4. 文档与知识库原则

- 文档不是附属品，而是项目的一部分；开发过程必须维护项目知识库。
- 根目录散落的新 Markdown 文档不长期保留；正式接入 doc-pilot 后应归档到 `docs/`。
- 需求原文与需求快照都要保留：
  - docx 是 source of truth
  - md 是 AI 可检索快照
- 若需求文档更新，后续应同步更新 md 快照，避免答非所问或基于过期需求开发。

## 5. 实现原则

- 严格遵循规格，不凭猜测补功能。
- 优先做局部、精确、可验证的修改，避免无关重构。
- 先交付能工作的最小闭环，再做增强，不追求一步到位的大而全实现。
- 配置驱动优先于硬编码，尤其是分类模板、摘要抽取、关联判定阈值等需求中已明确具备扩展性的部分。
- 单用户系统假设成立前，不引入多租户、复杂权限、企业级分布式治理等超范围设计。

## 6. 技术决策边界

- 检索层优先使用 PostgreSQL + pgvector + 全文检索，不擅自引入 Elasticsearch、Milvus、Qdrant 等额外基础设施，除非用户明确要求。
- 调度与异步优先沿用需求文档中的 `Spring @Scheduled + Redis` 思路，不擅自升级到 Kafka/RabbitMQ。
- 部署默认面向单机 Docker Compose，不提前引入 Kubernetes。
- 涉及第三方库、框架 API、枚举值、方法签名时，必须基于真实代码或真实文档核实；不能靠印象编码。

## 7. 用户协作约束

- 非必要不追问；能做合理假设时直接推进，并在结果中说明假设。
- 当存在非显然后果的方案分叉时，再暂停与用户确认。
- 不替用户执行 git add / commit / push，除非用户明确要求。
- 每次代码改动完成后，都要附上英文 commit message 草案，并列出本次建议 `git add` 的文件清单；若已有 Jira ticket，commit message 需带 ticket 编号前缀。
- 保持行动优先：不要在没有阻断证据时反复推翻已选技术路径。

## 8. 执行风格：拒绝过度思考，行动优先

适用前提：这条约束适用于技术实现决策效率，不适用于值守流程本身。像“先读文档”“先走初始化”“先查 bug 历史”这类流程性动作必须执行，不属于过度思考。

具体要求：

- 不在开始前做超长推演，优先拆小步推进。
- 有可落地路径时先产出代码或文档，不空转。
- 自动化验证以覆盖核心链路为主，不盲目追求过度测试。
- 一旦确定实现路径，除非遇到新的阻断性证据，否则不要反复推翻。

## 9. 长任务上下文保护

若任务持续时间长、上下文即将膨胀，主动建议用户开启新会话，并提供可直接复用的上下文摘要提示词。摘要内容至少包括：

- 项目核心技术栈
- 当前任务进度
- 已知坑点与约束
- 下一步计划

## 10. 本项目当前已知事实

- `doc-pilot` skill 已解压到 `.trae/skills/doc-pilot-v2/`
- 当前 `docs/` 知识库已初始化，并由 `docs/.doc-pilot.json` 管理
- 当前根目录仍保留需求文档原件与其 Markdown 快照
- 本项目已配置 Jira Cloud 站点 `wilfredchau.atlassian.net`，项目 Key 为 `SPKB`
- 当前开发前置建票 skill 为 `.trae/skills/dev-with-ticket/SKILL.md`

如后续项目结构、值守 skill 路径或流程发生变化，应优先更新本文件。
