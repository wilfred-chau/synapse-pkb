# 个人知识库系统（PKB）

## 需求规格说明书

Personal Knowledge Base with Proactive Memory

项目性质：个人独立开发 / AI全栈技能沉淀项目

版本：V1.0

编制人：周铨（Wilfred）

# 目录

- 一、项目背景与目标

- 二、术语与范围界定

- 三、系统模块一：基础平台

- 四、系统模块二：内容处理与分类感知摘要

- 五、系统模块三：检索

- 六、系统模块四：主动关联引擎（核心）

- 七、系统模块五：主动呈现

- 八、系统模块六：语音备忘录（二期）

- 九、通用功能

- 十、技术架构与技术栈

- 十一、数据模型设计

- 十二、非功能性需求与容量指标

- 十三、项目实施前提

- 十四、开发路线图与里程碑

- 十五、风险与应对

- 十六、交付物清单

# 一、项目背景与目标

## 1.1 背景

本人现从事AI应用全栈开发工作（Spring Boot 3 + React 18 + PostgreSQL 16技术栈），日常已具备Java后端、AI应用集成（LangChain4j、RAG、Agent）的实战经验。本项目为业余时间独立开发的个人项目，不与现有雇主工作内容挂钩，目标是通过一个真正会长期使用的日常工具，系统性补齐长期记忆系统设计、主动式Agent工程、多模态内容处理等当前工作中较少涉及的技能点。

## 1.2 核心问题

日常积累的技术文章、读书笔记、漫画观后感、观影记录分散在微信收藏、备忘录、脑内记忆中，读完即忘，彼此之间的关联从未被系统性地发现和利用。市面上的笔记类工具（Notion、语雀、Obsidian）解决的是「存储与被动检索」问题——用户需要主动搜索才能找到信息；而本项目要解决的是「主动关联」问题——系统在后台持续发现新旧知识之间的联系，并在合适的时机主动呈现给用户，形成真正意义上的「越用越懂你」的第二大脑。

## 1.3 项目目标

- (1) 工具目标：搭建一个支持多品类内容（技术资料、小说、漫画、电影、生活笔记）统一管理、语义检索、主动关联提示的个人知识库，实现每日可用。

- (2) 技能目标：完整掌握 Embedding 与向量检索工程化落地、分类感知的结构化信息抽取、长期记忆系统（主动关联、重要性衰减、记忆巩固）的设计与实现、AI Agent 定时任务编排。

- (3) 沉淀目标：每个开发阶段产出一篇技术复盘笔记，形成可迁移到本职工作汇报（AI-native开发方法论）的素材。

## 1.4 目标用户

仅面向本人使用，不考虑多租户、权限体系、商业化等企业级需求；架构预留后续向家庭成员（如伴侣共同使用）扩展的可能性，但不在本期范围内实现。

# 二、术语与范围界定

| 术语 | 定义 |
| --- | --- |
| 条目 (Entry) | 系统中最小的知识记录单元，对应一篇文章、一条笔记、一本书/一部漫画/一部电影的记录。 |
| 分类 (Category) | 条目的一级归属，本期固定为：技术资料 / 小说 / 漫画 / 电影 / 生活笔记，共5类。 |
| 关联 (Relation) | 系统自动发现的两个条目之间的语义联系，包含关联类型、置信度、生成理由三个核心属性。 |
| 主动关联引擎 | 本项目的核心模块：在新条目入库或定时任务触发时，自动召回候选条目并由LLM判定关联关系，无需用户主动搜索。 |
| 候选召回 | 在做LLM关联判定前，先用低成本方法（标签重叠、同分类、向量粗筛）缩小比对范围的步骤，避免对全库做O(n²)比对。 |
| 记忆衰减 | 关联的置信度或重要性随时间、未被访问的周期逐步降低并最终归档的机制，避免关联噪音无限累积。 |
| 待确认队列 | 置信度处于中间区间的关联不会直接展示，而是进入人工确认队列，用户的确认/忽略反馈用于校准后续判定阈值。 |

注：本文档中「本期」指第一期开发范围（Phase 1-4，详见第十四节），「二期」指语音输入相关的Phase 5，视精力投入情况排期。

# 三、系统模块一：基础平台

| 编号 | 功能项 | 详细说明 |
| --- | --- | --- |
| A1 | 用户认证与个人空间 | 单用户模式，JWT本地登录，不接入第三方OAuth；数据库层预留 user_id 字段与索引，为后续多用户扩展（如家庭共享）留出迁移路径，但本期所有查询均按固定单用户过滤。 |
| A2 | 分类与标签体系 | 一级分类固定5类（见第二节）；二级标签用户可自由创建、合并、重命名；条目入库时由AI基于内容生成3-5个候选标签供用户一键采纳或修改，减少手工打标签的摩擦。 |
| A3 | 统一录入入口 | 支持三种录入方式：①长文本直接粘贴；②URL抓取（自动提取正文，剔除广告与导航栏，基于Readability算法或类似的正文提取库）；③文件上传（.txt/.md/.pdf，PDF复用现有pdf处理经验做文本提取）。语音录入见模块F（二期）。 |
| A4 | 附件与原文存储 | 原始内容全文归档不做任何压缩摘要，保证「摘要生成逻辑变更后可重新生成」；图片类内容（漫画封面、电影海报）存储缩略图，原图链接可选存储，不做本地图床（控制存储成本）。 |
| A5 | 条目基础操作 | 增删改查、软删除（回收站30天）、批量导入（支持从Markdown文件批量导入历史笔记，逐条解析为独立Entry）。 |

# 四、系统模块二：内容处理与分类感知摘要

这是本项目区别于「通用RAG知识库」的第一个技术要点：不同类型的内容需要不同的信息抽取策略，而不是用同一套摘要模板处理所有内容。

| 编号 | 功能项 | 详细说明 |
| --- | --- | --- |
| B1 | 分类路由抽取框架 | 条目入库后根据 category 字段路由到对应的结构化抽取 Prompt 模板，模型以强制JSON Schema输出结构化元数据，写入 entries.metadata（JSONB）字段。 |
| B1.1 | — 技术资料类模板 | 抽取字段：核心概念(concepts[])、涉及技术栈(tech_stack[])、适用场景(use_case)、与既有知识的关键词重叠提示(overlap_hint)。 |
| B1.2 | — 小说/漫画类模板 | 抽取字段：作者(author)、主要角色(characters[])、核心主题(themes[])、情绪基调(mood，如「压抑/治愈/热血」)、当前阅读进度(progress)。 |
| B1.3 | — 电影类模板 | 抽取字段：导演(director)、类型(genre[])、个人评分(rating，用户可后填)、观后感倾向(sentiment)、观影日期(watched_at)。 |
| B1.4 | — 生活笔记类模板 | 抽取字段：涉及人物/场景(entities[])、情绪标签(mood)、是否含待办事项(has_todo)。 |
| B2 | 统一摘要生成 | 无论分类，统一生成「一句话摘要」（用于列表展示，≤40字）与「三点摘要」（用于详情页与后续关联理由生成的上下文）。 |
| B3 | Embedding生成与索引 | 摘要文本 + 结构化元数据拼接后送入Embedding模型，写入 pgvector 向量列；模型可切换（GLM Embedding API 或本地 bge-large-zh），切换后支持批量重新生成全库向量。 |
| B4 | 内容去重检测 | 入库前用向量相似度（阈值≥0.95）检测高度相似的已有条目，提示用户「是否与已有条目合并」，避免重复收藏同一篇文章造成噪音。 |

注：B1.1-B1.4 的模板设计是可扩展的：新增内容分类只需新增一个模板配置（JSON文件或数据库配置表），不需要改动核心抽取流程代码，这本身也是一次「配置驱动 vs 硬编码」的工程实践。

# 五、系统模块三：检索

| 编号 | 功能项 | 详细说明 |
| --- | --- | --- |
| C1 | 语义检索 | 基于 pgvector 的余弦相似度检索，输入自然语言query，返回Top-K相关条目；支持按分类/标签预过滤后再做向量检索，减少候选集。 |
| C2 | 关键词检索 | PostgreSQL 原生全文检索（tsvector + GIN索引），处理专有名词、人名、书名等语义检索容易漏召回的精确匹配场景。 |
| C3 | 混合排序 | 最终排序分数 = 语义相似度权重(0.6) + 关键词命中权重(0.25) + 时间新鲜度衰减权重(0.15)，权重可配置，为后续A/B调优留接口。 |
| C4 | 高级筛选面板 | 前端提供分类、标签（多选）、时间范围、评分区间的组合筛选，配合 Ant Design 的 Filter/Table 组件实现。 |

# 六、系统模块四：主动关联引擎（核心模块）

本模块是全项目技术含量最高、也是与「被动RAG」本质区别所在的部分，独立成节详细说明。

| 编号 | 功能项 | 详细说明 |
| --- | --- | --- |
| D1 | 候选召回 | 新条目入库时触发。先用「同分类 + 标签重叠数≥1」做粗筛，若候选数仍过多（>50），再用向量相似度取Top-30，避免对全库做逐条LLM比对（成本不可控）。 |
| D2 | 关联判定与打分 | 对每个候选pair，调用LLM做结构化判断，强制输出JSON：{relation_type: 主题延伸\|观点对比\|引用关系\|情绪呼应\|无关, confidence: 0-1, reasoning: 一句话理由}。 |
| D3 | 待确认队列 | confidence ∈ [0.4, 0.75) 的关联进入 pending_review 状态，用户在「待确认关联」页面批量滑动确认/忽略；confidence ≥ 0.75 直接标记 confirmed 并在信息流中呈现；confidence < 0.4 直接丢弃不落库。 |
| D4 | 记忆重要性与衰减 | 每条confirmed关联维护一个 importance_score，初始值=confidence；用户每次点击查看该关联 +0.1（上限1.0）；每30天未被访问 -0.05；低于0.2的关联自动归档（不删除，仅从主视图隐藏，保留可追溯）。 |
| D5 | 定时任务调度 | 基于 Spring @Scheduled + Redis 任务队列实现削峰：新条目入库先入队，由后台worker异步消费做D1-D2计算，避免阻塞用户的录入操作体验。 |
| D6 | 关联去重 | 同一对条目（entry_id_a, entry_id_b 无序对）只保留一条关联记录，重复触发时更新而非新增。 |

注：D3的三段阈值（0.4 / 0.75）是初始经验值，规划在Phase 3上线后收集至少100条人工确认反馈，用于校准或引入简单的在线学习（如根据用户确认率动态微调阈值）。

# 七、系统模块五：主动呈现

| 编号 | 功能项 | 详细说明 |
| --- | --- | --- |
| E1 | 每周知识回顾 | 每周一定时生成「本周新增关联」「被重新激活的旧知识」两类卡片流，通过站内信息流呈现（不做邮件推送，避免打扰）。 |
| E2 | 知识关联图谱可视化 | 条目为节点、关联为边的力导向图，前端使用 AntV G6 或 D3.js 实现；点击边可展开关联理由；节点大小映射条目被关联次数（度数）。 |
| E3 | 首页信息流 | 混合排序展示：待确认关联（优先级最高）+ 近期高置信度新关联 + 每周回顾摘要，类似一个「知识版朋友圈」。 |

# 八、系统模块六：语音备忘录（二期）

| 编号 | 功能项 | 详细说明 |
| --- | --- | --- |
| F1 | 语音录入与转写 | 前端录音（Web Audio API）→ 上传音频 → 调用ASR（本地Whisper或云端API二选一，视一期成本情况决定）转写为文字 → 复用模块B（B1-B4）的全部处理流程，不新增独立的处理链路。 |
| F2 | 语音原文回放 | 转写文本与原始音频文件关联存储，详情页保留播放入口，便于校对AI转写是否有误。 |

注：语音模块刻意设计为「录入方式的扩展」而非独立子系统，正是为了验证A3/Module B设计的可扩展性——好的架构应当让新增一种输入源的成本趋近于零。

# 九、通用功能

| 编号 | 功能项 | 详细说明 |
| --- | --- | --- |
| G1 | 数据导出 | 列表/单条目导出 Markdown 或 JSON，用于备份和迁移，避免被平台锁定；导出内容包含原文、摘要、元数据、关联关系。 |
| G2 | 操作日志 | 记录条目的编辑/删除/关联确认等关键操作，供后续行为分析使用（如「你最常确认哪类关联」用于个性化调优）。 |
| G3 | 定时任务框架 | 复用D5的调度基础设施，扩展支持：每日数据库备份、Embedding模型迁移后的批量重算任务。 |
| G4 | API文档 | 基于 Springdoc OpenAPI 自动生成接口文档，为后续可能的移动端/浏览器插件预留标准化接口契约。 |

# 十、技术架构与技术栈

## 10.1 总体架构

系统采用前后端分离的分层架构，按「接入层—应用层—服务层—数据层」设计，Docker容器化，部署于现有内网服务器（CentOS Stream 9，地址：192.168.106.130），延续单机部署与反向代理思路。

| 层次 | 内容 | 技术选型 |
| --- | --- | --- |
| 接入层 | Web客户端、静态资源托管 | React 18 + TypeScript + Ant Design 5；Vite构建；Axios请求封装；AntV G6（知识图谱可视化） |
| 应用层 | 内容管理、检索、呈现三大模块的REST接口 | Java 17 + Spring Boot 3；Spring Web MVC；Springdoc OpenAPI |
| 服务层 | 认证、AI能力封装、任务调度、关联计算引擎 | Spring Security（JWT）；GLM API封装层（Embedding + Chat双接口）；Spring @Scheduled；自研候选召回与关联打分逻辑 |
| 数据层 | 结构化数据、向量索引、任务队列与缓存 | PostgreSQL 16 + pgvector 扩展；Redis（任务队列 + 缓存 + 会话） |
| 部署运行 | 容器化部署、配置管理 | Docker + Docker Compose；现有 CentOS Stream 9 服务器（192.168.106.130）单节点部署；Nginx反向代理 |

注：本期不引入的组件：Elasticsearch（PostgreSQL全文检索已够用）、Kubernetes（单人单节点无需编排）、消息队列中间件如Kafka/RabbitMQ（Redis队列已满足削峰需求）。如后续数据量或并发需求增长，架构在服务层预留升级接口。

## 10.2 关键第三方依赖

| 依赖 | 用途 | 备选方案 |
| --- | --- | --- |
| GLM Embedding API | 文本向量化 | 本地部署 bge-large-zh（省成本但需占用服务器算力） |
| GLM Chat API（4.7-flash / 5.3） | 结构化摘要、关联判定，复用你现有的双模型workflow经验 | — |
| pgvector | PostgreSQL向量存储与相似度检索扩展 | Milvus/Qdrant独立向量库（本期数据量下pgvector足够，避免多一套中间件运维） |
| Readability4J 或类似正文提取库 | URL抓取时剔除网页噪音 | Jsoup手写规则（更可控但维护成本高） |

# 十一、数据模型设计

## 11.1 核心表：entries（条目）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGSERIAL | 主键 |
| category | VARCHAR(20) | tech / novel / comic / movie / note |
| title | VARCHAR(255) | 标题 |
| raw_content | TEXT | 原文全文 |
| summary_one_line | VARCHAR(100) | 一句话摘要 |
| summary_points | TEXT[] | 三点摘要 |
| tags | TEXT[] | 标签数组 |
| metadata | JSONB | 分类专属结构化元数据（见第四节B1.1-B1.4） |
| embedding | VECTOR(1024) | pgvector向量列，维度视所选Embedding模型调整 |
| source_url | VARCHAR(500) | 来源链接，可为空 |
| created_at / updated_at | TIMESTAMP | 创建/更新时间 |
| deleted_at | TIMESTAMP | 软删除标记，为空表示未删除 |

## 11.2 核心表：relations（关联）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGSERIAL | 主键 |
| entry_id_a / entry_id_b | BIGINT | 关联的两个条目ID，(a,b)按id大小排序存储保证唯一性 |
| relation_type | VARCHAR(20) | 主题延伸 / 观点对比 / 引用关系 / 情绪呼应 |
| confidence | FLOAT | LLM判定的置信度，0-1 |
| reasoning | VARCHAR(255) | AI生成的关联理由（一句话） |
| importance_score | FLOAT | 动态重要性分数，用于排序与衰减（见D4） |
| status | VARCHAR(20) | pending_review / confirmed / dismissed / archived |
| last_viewed_at | TIMESTAMP | 最后一次被用户查看的时间，用于衰减计算 |
| created_at | TIMESTAMP | 创建时间 |

## 11.3 辅助表

| 表名 | 用途 |
| --- | --- |
| tags | 标签主表，维护标签的使用次数统计，用于标签云展示与自动推荐排序 |
| operation_logs | 操作日志表，记录G2所需的用户行为数据 |
| scheduled_jobs | 定时任务执行记录表，记录D5/G3任务的执行状态与失败重试次数 |

# 十二、非功能性需求与容量指标

| 指标 | 本期目标 |
| --- | --- |
| 数据规模假设 | 条目数 ≤ 5万条（按每日3-5条、持续3-5年估算）；标签数 ≤ 500个 |
| 语义检索响应 | 单次检索（含Embedding API调用）≤ 2秒 |
| 关联计算时延 | 单条目新增触发的关联计算异步完成，不阻塞录入操作；目标处理时长 ≤ 30秒/条 |
| Embedding成本控制 | 月度API调用量预算封顶（建议设置GLM账户消费预警），超预算自动降级为本地embedding模型 |
| 存储容量 | 数据库+对象存储预留 ≤ 20GB（纯文本为主，不存储大量原图/音频） |
| 并发 | 单用户使用场景，无并发压力，不做负载测试 |
| 数据安全 | 个人隐私笔记类内容，服务器仅本人可访问，不对公网开放注册；定期本地/云端双备份 |

# 十三、项目实施前提

| 编号 | 前提 | 说明 |
| --- | --- | --- |
| 1 | 独立开发 | 本项目由本人利用业余时间独立完成，无团队协作与需求变更审批流程，但仍按「需求—设计—开发—测试—复盘」的完整工程规范执行，服务于学习目标。 |
| 2 | API依赖与成本 | 核心AI能力（Embedding、摘要生成、关联判定）依赖GLM API调用，需设置月度预算上限，超限触发降级策略（本地模型或延迟处理）。 |
| 3 | 现有服务器复用 | 沿用现有 CentOS Stream 9 服务器（192.168.106.130） + Docker Compose 部署方式，需评估现有配置（CPU/内存/存储）是否满足 PostgreSQL+pgvector+Redis+应用服务的并行运行，如不足需评估升配成本。 |
| 4 | 学习目标优先于交付速度 | 允许有意选择学习价值更高但开发周期更长的技术路径（如自研候选召回算法而非直接调用现成框架），每个里程碑需输出对应的学习复盘笔记。 |
| 5 | 历史数据迁移 | 本期不做微信收藏/其他平台笔记的自动化爬取同步，历史内容通过手动批量导入（Markdown文件批量导入功能，见A5）迁移。 |
| 6 | 范围冻结 | 第三至九节功能项为本期（Phase 1-4）与二期（Phase 5）范围边界，开发过程中如产生新增想法，记入Backlog而非中途插入当前Phase，保证阶段可按期收口。 |

# 十四、开发路线图与里程碑

| 里程碑 | 内容 | 目标周期 | 阶段出口条件 | 学习产出 |
| --- | --- | --- | --- | --- |
| M1 | Phase 1：基础平台联调（模块A + B3基础版） | T+3周 | Entry CRUD、Embedding写入、基础语义检索可用，可通过Postman验证全链路 | pgvector实战、Spring Data JPA与向量类型映射 |
| M2 | Phase 2：分类感知摘要（模块B全量） | T+5周 | 四类内容的差异化Prompt模板上线，人工抽查20条摘要/元数据准确率≥85% | 结构化输出（JSON Schema约束生成）、领域自适应Prompt设计 |
| M3 | Phase 3：主动关联引擎v1（模块D） | T+9周 | 候选召回、LLM关联打分、待确认队列全部上线，跑通100条历史数据的批量关联计算 | 长期记忆系统设计、异步任务削峰、候选召回算法 |
| M4 | Phase 4：主动呈现（模块E） | T+11周 | 每周回顾页面、知识图谱可视化上线，前端体验可日常使用 | 前端数据可视化（AntV G6）、信息流混合排序算法 |
| M5 | Phase 5：语音备忘录（模块F，二期） | T+14周（视精力浮动） | ASR接入打通，语音条目与文本条目共用同一套处理链路 | 语音全链路工程（ASR流式处理）、验证架构可扩展性 |

注：周期以「本项目启动时点T」为基准，均为业余时间投入下的预估值，允许根据本职工作强度动态调整，但每个Phase收尾时必须产出对应复盘笔记，保证学习目标不因赶工被牺牲。

# 十五、风险与应对

| 风险 | 说明 | 应对措施 |
| --- | --- | --- |
| 精力投入不稳定 | 业余项目容易因本职工作繁忙而停滞 | 每个Phase拆解为周粒度任务，允许单周产出为零，但不允许连续4周无提交 |
| 关联噪音过多 | D2的LLM判定可能产生大量低价值关联，反而增加信息过载 | 严格执行D3的三段阈值机制，且D4衰减机制强制清理低价值关联 |
| API成本失控 | 关联计算是O(候选数)级别的LLM调用，条目量增长后成本可能超预期 | D1候选召回严格限流（Top-30以内），并设置月度预算硬上限 |
| 过度设计 | 作为学习项目容易陷入「什么都想学一遍」导致范围蔓延 | 严格按第十四节Phase边界执行，新想法进Backlog不进当前Phase |

# 十六、交付物清单

| 编号 | 交付物 | 形式 | 阶段 |
| --- | --- | --- | --- |
| D1 | 需求规格说明书（本文档） | Word / Markdown | P0 |
| D2 | 数据库设计文档（ER图 + 建表脚本） | SQL + Markdown | M1 |
| D3 | API接口文档 | OpenAPI / Swagger | M1-M4持续更新 |
| D4 | 源代码 | Git仓库（GitHub） | 全程 |
| D5 | 应用系统 | Docker镜像 + docker-compose部署包 | M1-M4 |
| D6 | 各Phase技术复盘笔记 | Markdown，5篇 | 每个里程碑收尾 |
| D7 | AI-native开发方法论素材 | 可复用于本职工作汇报的PPT/文档片段 | M3-M4 |
